<h1><%= @task.title %></h1>

<div><%= @task.details %></div>

<div><%= link_to "Edit task", edit_task_path(@task) %></div>

<div><%= link_to "Delete task", task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></div>

<div><%= link_to "Back to list", root_path %></div>
